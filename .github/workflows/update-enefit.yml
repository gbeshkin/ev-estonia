name: Update Enefit Volt data

on:
  schedule:
    - cron: "0 6,18 * * *"
  workflow_dispatch:

permissions:
  contents: write

jobs:
  update:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          persist-credentials: true

      - name: Setup Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.11"

      - name: Install dependencies
        run: pip install requests

      - name: Fetch live Enefit data
        env:
          ENEFIT_CSRF_TOKEN: ${{ secrets.ENEFIT_CSRF_TOKEN }}
          ENEFIT_COOKIE: ${{ secrets.ENEFIT_COOKIE }}
          ENEFIT_REFERER: ${{ secrets.ENEFIT_REFERER }}
        run: python scripts/fetch_enefit_live.py

      - name: Commit updated data
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add data/chargers.json
          git diff --cached --quiet || git commit -m "Update Enefit Volt data"
          git push
